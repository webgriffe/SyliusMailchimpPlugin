<div class="card g-col-12 g-col-xl-6">
    <div class="card-header">
        <div class="card-title">{{ 'webgriffe_sylius_mailchimp.ui.mailchimp'|trans }}</div>
    </div>

    <div class="card-body">
        <div class="row">
            {{ form_row(form.mailchimpAudienceId) }}
            {{ form_row(form.mailchimpNewsletterPositions) }}
        </div>
    </div>
</div>
